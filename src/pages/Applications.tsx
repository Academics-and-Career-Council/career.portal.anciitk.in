import Wrapper from "../components/Wrapper"

const Applications = () => {
  const jsx = (
    <div>
      Your Applications
    </div>
  )

  return <Wrapper component={jsx} />
}

export default Applications